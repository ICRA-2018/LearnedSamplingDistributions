name: learnedsamplingdistributions

base:
  system: ubuntu
  version: 16.04
  cuda: 8.0
  cudnn: 6

apt-packages:
  - python-setuptools
  - python-dev

pip-packages:
  - tensorflow-gpu==1.4
  - matplotlib==2.2.3
  - ipython==5.8.0
  - ipykernel==4.10.0

custom:
  - python -m ipykernel install
